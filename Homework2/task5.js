function sum(x, y) {
    return (x + y);
}
sum = sum(10, 20);
console.log(sum);

function sub(x, y) {
    return (x - y);
}
sub = sub(10, 20);
console.log(sub);

function devis(x, y) {
    return (x / y);
}
devis = devis(10, 20);
console.log(devis);

function multy(x, y) {
    return (x * y);
}
multy = multy(10, 20);
console.log(multy);
